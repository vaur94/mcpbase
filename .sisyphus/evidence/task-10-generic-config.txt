Task 10 - Generic loadConfig<TConfig>(schema, options)

Modified files:
- src/config/load-config.ts
- tests/unit/config-loader.test.ts

Implemented:
- loadConfig now supports generic schema + LoadConfigOptions
- envPrefix is configurable (default: MCPBASE_)
- defaultConfigFile is configurable (default: mcpbase.config.json)
- 4-layer merge order remains: default -> file -> env -> CLI
- backward compatibility preserved for legacy loadConfig(argv)

Verification:
- npx vitest run tests/unit/config-loader.test.ts -> PASS (3/3)
- npx tsc --noEmit -> PASS
- lsp_diagnostics -> no errors in modified files
