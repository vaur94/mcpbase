# Task 6 Evidence: Dependabot Auto-Merge Workflow Created

## Workflow Created
File: .github/workflows/dependabot-auto-merge.yml

## Verification Checklist

### Trigger
- [x] Triggers on pull_request event

### Actor Condition  
- [x] Only runs for github.actor == 'dependabot[bot]'
- Line 10: `if: github.actor == 'dependabot[bot]'`

### Update Type Detection
- [x] Uses dependabot/fetch-metadata@v2
- Line 17: `uses: dependabot/fetch-metadata@v2`

### Major Version Exclusion
- [x] Skips if update-type is 'version-update:semver-major'
- Line 22: `if: steps.metadata.outputs.update-type != 'version-update:semver-major'`

### Auto-Approve
- [x] Uses `gh pr review --approve`
- Line 24: `gh pr review --approve "$PR_URL"`

### Auto-Merge
- [x] Uses `gh pr merge --auto --squash`
- Line 25: `gh pr merge --auto --squash --delete-branch "$PR_URL"`

### Permissions
- [x] contents: write
- [x] pull-requests: write

### Token
- [x] GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
- Line 28: `GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}`

## YAML Validation
- YAML syntax validated successfully with js-yaml

## Commands Verified
- gh pr review --approve - approves the PR
- gh pr merge --auto --squash -- enables auto-merge with squash strategy
