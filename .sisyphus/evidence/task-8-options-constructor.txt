Task 8 Evidence: Options Constructor
=====================================
Date: 2026-03-07

ApplicationRuntime now uses RuntimeOptions<TConfig, TContext> interface:

```typescript
export interface RuntimeOptions<
  TConfig extends BaseRuntimeConfig = BaseRuntimeConfig,
  TContext extends BaseToolExecutionContext<TConfig> = BaseToolExecutionContext<TConfig>,
> {
  readonly config: TConfig;
  readonly logger: Logger;
  readonly tools: ToolDefinition<any, any, TContext>[];
  readonly contextFactory?: (toolName: string, requestId: string, config: TConfig) => TContext;
}
```

Constructor changed from positional args to options object:
- OLD: new ApplicationRuntime(config, logger, tools)
- NEW: new ApplicationRuntime({ config, logger, tools })

All callers updated:
- src/index.ts (bootstrap function)
- tests/unit/runtime-normalization.test.ts
- tests/unit/transport.test.ts
- tests/unit/example-tools.test.ts
- tests/integration/runtime.test.ts

RuntimeOptions is exported from src/application/runtime.ts and re-exported from src/index.ts.

Verification:
- npx tsc --noEmit → zero errors
- npx vitest run → 96 tests pass (16 files)
